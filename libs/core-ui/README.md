# @pos/core-ui

Reusable core UI building blocks for POS applications.

## Included

- `SnackbarService` (root service)
- `SnackbarOutletComponent` (global stacked snackbar renderer)

## Usage

```ts
import { SnackbarService, SnackbarOutletComponent } from '@pos/core-ui';
```
