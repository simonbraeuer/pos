# Pending Payment Default

Fallback overlay for payment types that don't have specialized handlers (Bank Transfer, Direct Debit, Digital Wallet, Other).

Auto-finalizes the payment with retry and abort capabilities on failure.
