{
  "name": "idb-storage",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/idb-storage/src",
  "prefix": "pos",
  "projectType": "library",
  "tags": ["scope:core-utils"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/libs/idb-storage"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
