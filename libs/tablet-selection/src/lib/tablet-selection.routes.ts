import { Routes } from '@angular/router';
import { TabletSelectionComponent } from './tablet-selection.component';

export const TABLET_SELECTION_ROUTES: Routes = [
  {
    path: 'tablet-selection',
    component: TabletSelectionComponent,
  },
];
